---
name: veda-heuristic
description: >
  Alias for LENS mode (formerly HEUR). Apply one thinking lens, powered by a stateless
  specialist. Use veda-lens going forward.
type: workflow
owner: veda
---

# veda-heuristic → LENS (alias)

`HEUR` is the legacy name for **LENS**. This skill is a thin alias for backward compatibility.

**Run `{module-root}/modes/lens.md`** (skill: `veda-lens`). Lens codes resolve via
`{module-root}/lenses/index.md` → `veda-agent-{slug}`, which runs Teach → Model → Practice.

> Migration: prefer `LENS` / `veda-lens`. The "specialist roster" is now the **Lens Library**.
> See `{module-root}/docs/migration-v2.md`.
