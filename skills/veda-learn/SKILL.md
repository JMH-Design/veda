---
name: veda-learn
description: >
  Alias for BUILD mode (formerly LEARN). Build one durable mental model of a topic with Veda.
  Use veda-build going forward.
type: workflow
owner: veda
produces: learning-artifact.md
---

# veda-learn → BUILD (alias)

`LEARN` is the legacy name for **BUILD**. This skill is a thin alias for backward compatibility.

**Run `{module-root}/modes/build.md`** (skill: `veda-build`). All BUILD behavior — the
seven-phase lesson, the Tutor Loop, the Mastery Card — lives there.

> Migration: prefer `BUILD` / `veda-build`. See `{module-root}/docs/migration-v2.md`.
