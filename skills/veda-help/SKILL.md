---
name: veda-help
description: >
  Alias for NEXT mode (formerly HELP). Inspect learner state and recommend the next best move.
  Use veda-next going forward.
type: task
owner: veda
---

# veda-help → NEXT (alias)

`HELP` is the legacy name for **NEXT**. This skill is a thin alias for backward compatibility.

**Run `{module-root}/modes/next.md`** (skill: `veda-next`).

> Migration: prefer `NEXT` / `veda-next`. See `{module-root}/docs/migration-v2.md`.
